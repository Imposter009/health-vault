package com.healthvault;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertTrue;

/**
 * Phase 0 placeholder — no Spring context needed yet (no running DB in CI).
 * Real integration tests are added in Phase 1 alongside the first entities.
 */
class HealthVaultApplicationTests {

    @Test
    void placeholder() {
        assertTrue(true, "Phase 0 scaffold is in place");
    }
}
