{
  "id": "b1e6f0a0-4b7a-4b0a-9b2a-health-vault-local",
  "name": "health-vault - Local",
  "values": [
    {
      "key": "baseUrl",
      "value": "http://localhost:8081",
      "type": "default",
      "description": "Gateway URL (Phase 5 intended entry point). Use http://localhost:8080 to hit the Core API directly, bypassing the gateway's rate limiting and CORS handling, for debugging only.",
      "enabled": true
    },
    {
      "key": "accessToken",
      "value": "",
      "type": "secret",
      "description": "Auto-populated by the Login/Refresh Token test scripts. Do not set manually.",
      "enabled": true
    },
    {
      "key": "refreshToken",
      "value": "",
      "type": "secret",
      "description": "Auto-populated by the Login/Refresh Token test scripts. Do not set manually.",
      "enabled": true
    },
    {
      "key": "userEmail",
      "value": "testuser@healthvault.local",
      "type": "default",
      "description": "TEST-ONLY placeholder credential. The Register request's pre-request script overwrites this with a unique value (timestamp-suffixed) on each run so the collection can be re-run from scratch without hitting a duplicate-email 409. Never use a real email here.",
      "enabled": true
    },
    {
      "key": "userPassword",
      "value": "TestPassw0rd!123",
      "type": "secret",
      "description": "TEST-ONLY placeholder credential, satisfies the 8-character minimum. Never use a real password here.",
      "enabled": true
    },
    {
      "key": "documentId",
      "value": "",
      "type": "default",
      "description": "Auto-populated by the Upload Document test script from the created document's id.",
      "enabled": true
    },
    {
      "key": "metricId",
      "value": "",
      "type": "default",
      "description": "Auto-populated by the Create Metric test scripts from the created metric's id (last one created wins).",
      "enabled": true
    }
  ],
  "_postman_variable_scope": "environment"
}
